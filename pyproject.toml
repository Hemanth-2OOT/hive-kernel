[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hive"
version = "0.1.0"
description = "Fault-tolerant local AI orchestration microkernel"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
include = ["hive*"]
